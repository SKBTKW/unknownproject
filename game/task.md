# タスクリスト: マジェスタ開発 (ComfyUI風キャンバス＆超高画質ビジュアル・自作素材対応完了)

- [x] 3Dクォータービュー＆8スロット接続型ボード（ComfyUI風）の実装
  - [x] 8スロットデータモデル (`state.js`) への移行（スロット0＝宮殿、スロット1〜7＝自由配置土地）
  - [x] 土地カードの最大所有上限数 `7`（分母 `/7`）を完全に有効化
  - [x] 宮殿ノード of ロール別名称割り当てと固定産出（🌾10/🧱10/🛡️10）の実装
  - [x] 3DアイソメトリックCSSおよび上面・左面・右面の立体キューブ描画 (`style.css`)
  - [x] フラットな2DビルボードHUDオーバーレイ（レベル、産出量、特性、災害表示）の描画 (`components.js`)
  - [x] 土地のカード台座（.iso-card-base）を実装し、フォントサイズを **`16px`** にスケールアップして視認性を劇的に向上
  - [x] 浮遊アニメーションを廃止し、グローホバー効果へ移行
- [x] HTML5 Drag & Drop をベースにした自由位置配置＆ドラッグ移動
  - [x] ドローエリア/保留エリアからメインボードキャンバス上の任意のドロップ位置（%座標）への自由配置・アクティブ化の実装
  - [x] ドロップされた座標に `x, y` データを格納し、CSS `left`, `top` のインライン指定で描写 (`components.js`)
  - [x] クリックベースの配置モードにおいて、メインボード上の任意のクリック座標への配置処理の実装
  - [x] 配置済みの土地をドラッグし、メインボード上の別位置に動かすリアルタイム再配置ドラッグの実装
  - [x] HTML5 dragover 内でドラッグ中のスロットの座標をリアルタイムに更新し、追従移動させる高パフォーマンス処理
  - [x] 同種土地の上に重ねてドロップした際のマージ（ニコイチ）処理との統合
  - [x] ボード上の土地を保留スロットへドラッグした際に手札に戻し、`x, y` 座標を `null` にリセットする処理
- [x] 動的な接続線（SVGベジエ曲線）のレンダリング
  - [x] 中央 of 宮殿 (50%, 50%) から活性化された各土地 (x%, y%) への曲線パス of 生成 (`updateConnectionsSVG`)
  - [x] ComfyUI のような滑らかなS字を描く3次ベジエ曲線（Cubic Bezier Path）による描画
  - [x] 土地ノードドラッグ中のリアルタイムな接続線の追従・再描画
  - [x] 土地が災害/荒廃状態の際、自動的に接続線を細い赤の点滅線へ切り替える視覚フィードバック
- [x] 自作素材（透過画像）を用いた3Dノードビジュアル化対応
  - [x] 画像が存在すればそれを優先表示し、存在しなければ標準3Dキューブにフォールバックする `onload`/`onerror` トリガーの確立
  - [x] 画像の透過境界線に自動で輪郭シャドウを生成する `filter: drop-shadow` 陰影処理の追加 (`style.css`)
  - [x] マウスホバー時に土地モデルがスッと浮き上がる浮遊マイクロアニメーションを導入
- [x] 【NEW】高品質ゲームアート＆イラスト表現の導入
  - [x] 生成AI（`generate_image`）を用いて平地、丘陵、山岳、湖、宮殿の幻想的な風景イラストを全種生成
  - [x] 2Dカード中央に「イラスト描画エリア（.card-image-area）」を追加し、TCGのようなハイクオリティカードへアップグレード
  - [x] 3D土地ノードの上面（.iso-face.top）に、生成イラストをテクスチャとして背景マッピング
  - [x] 上面の中央絵文字を廃止し、美麗な景観ミニチュアとしての整合性を向上
  - [x] 3D側面（左面・右面）に、土壌から暗黒の地層へと沈むシームレスな深層グラデーションを適用
- [x] 配置・開発時の即時産出システム
  - [x] 土地の新規配置時にそのスロットの産出量を即時にプレイヤーに加算するロジック (`main.js`)
  - [x] 土地のニコイチ開発（結合・直接開発）完了時に、開発後のパラメータの産出量を即時にプレイヤーに加算するロジック
  - [x] `state.js` 内のターン終了生産制限を解除
- [x] 15ターン目の試練実行不能バグの解消
- [x] 試練「戦場の記憶」オーバーレイと荒廃ダメージループ (`trials.js`)
- [x] 結合時のビジュアルFX
- [x] 場の状況を反映したドローコントロール（選出システム）の実装
- [x] UIエリアとカードのサイズ拡張（サイズのみ2倍、枚数・保持数は3に維持）
- [x] ログ表示システムの改善と折りたたみ機能の追加
- [x] 土地の3Dホバーデータ（パラメータ・バッジ等）の視認性向上
- [x] 社会制度/政体システムの実装
- [x] 災害（☠️）カテゴリの整理と発生率の調整 (03_crisis.md 準拠)
  - [x] 疫病（Plague）および飢饉（Famine）の完全削除
  - [x] 亜人集団の襲撃（`crisis-raid`）の選択モーダル UI (`modal-raid-event`) の実装（買収か拒否かの二択）
- [x] 試練（Trial）戦闘システムの双方向・対話型ウェーブ戦闘シミュレーター化
  - [x] 兵士状態管理の定義 (`state.js` に `soldiers` 配列と `soldierCapacity` ゲッターを追加)
  - [x] 兵舎（Barracks）での毎ターン1名自動補充、および兵士全滅時の義勇兵1名自動補充ロジックの実装
  - [x] 試練戦闘準備 ＆ 兵士配備 UI モーダル (`modal-trial-battle`) の構築
  - [x] 準備フェイズ中、`1 🔥` を消費して即座に新兵を待機プールへ徴兵する機能の実装
  - [x] ウェーブごとの戦闘解決ログを時間差でタイピング出力するスリリングな演出の実装
  - [x] 戦闘後の兵士の生存確率判定、経験値（XP）獲得、レベルアップ（Lv+1）ロジックの実装
