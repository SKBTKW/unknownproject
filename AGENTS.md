# 🚨 PROJECT GLOBAL RULES (プロジェクト絶対遵守ルール)

本プロジェクト（Trial of Ages: Last Ember）において、AI（アシスタント）がどのような会話セッション、どのような指示、どのような開発ステップを実行する場合であっても、以下の **8大絶対鉄則** を永久かつ厳格に遵守しなければならない。

---

## 1. 🧪 【実機テストの義務】(パターン推測報告の絶対禁止)
* **反省点**: コードの見た目や文字パターンだけを見てブラウザテストをサボり、「問題ありません」「100%正常動作しています」と偽りの報告を行った結果、探索ボタンを押すと手札がリロールされる重大な重複イベントバグを見落とした。
* **鉄則**:
  1. ソースコードの構文が正しく見えても、**「パターンだけで動いていると判断すること」を100%禁止**する。
  2. 必ずブラウザ起動、実際のイベント発火、クリック動作、変数推移を実地で動的検証・目撃した上でしかユーザーへ報告してはならない。

---

## 2. 🧮 【1マス基礎産出公式の一貫適用】(勝手なデータ破棄・改ざんの全廃)
* **反省点**: ファイル上書き時に過去ログで確定していた正確なカードデータを読み込まず、適当な仮数値（🌾+30, 🧱+45, 🛡️+15等）で上書き破壊した。
* **鉄則**:
  1. すべての土地カードの産出数値は、例外なく以下の数学公式から自動算出すること。
     `カード総産出 ＝ (GL基礎産出 × 高度防衛倍率) × マス数`
  2. 直感やその場の思いつきでカードデータを勝手に書き換えることを全廃する。

---

## 3. 🌐 【ローカライズ4大約束の厳守】(テキストハードコーディングの禁止)
* **反省点**: 合意済みであった多言語ローカライズ方針を失念し、プログラム内に日本語テキストを直接ハードコーディング（ベタ書き）していた。
* **鉄則**:
  1. コード内に日本語テキストを直接埋め込むことを絶対禁止とし、必ず `I18N_DICTIONARY` のテキストIDキー (`CARD_PLAINS_1X3` 等) から `t(key)` 経由で取得すること。
  2. 言語ごとの語順差異を吸収するため、文字列の足し算（`"..." + val`）を禁止し、プレースホルダー `{0}` 形式へ統一すること。
  3. テキスト枠の固定幅を禁止し、可変幅レイアウトとすること。

---

## 4. 🃏 【確定ゲームルールの100%コード全実装】(未実装放置の禁止)
* **反省点**: 「選択したドロー以外のカードは選択不可」「全9大ブロック形状パターン」などの決定済みルールを未実装のまま放置していた。
* **鉄則**:
  1. ユーザーと合意・確定したゲームルール（単一選択ロック・全9大形状パターン等）は、遅滞なくプロトタイプおよびC#設計へ100%完全実装すること。

---

## 5. 📖 【マスター仕様書の初手強制ロード ＆ 捏造報告の禁止】
* **反省点**: 過去ログを深く検索せず、その場の文脈から勝手な偽物の「制作方針」を捏造して報告した。
* **鉄則**:
  1. 新しい会話セッションが開始された際、AIは作業を開始する前に必ず `rules/00_master_handover_specification.md` をロードして全確定事項を把握すること。
  2. 不確実な記憶や推測で適当な方針やルールを捏造して回答することを絶対に禁止する。

---

## 6. 📝 【事前コンペア提示の絶対義務 ＆ 仕様書無断要約・削除の 100% 全廃】
* **反省点**: 決定事項を仕様書へ書き込むのを怠り、見落としを放置してユーザーに再点検させる失態を犯した。また、仕様書更新時に過去の記述を勝手に要約・削る大罪を犯した。
* **鉄則**:
  1. ファイル編集を行う際は、必ず事前に **「変更前」と「変更後」の対比（事前コンペア）** をチャット上に提示し、ユーザーの明確な承認を得てから実行すること。
  2. 会話で決定したルールは即座に `rules/` 配下の仕様書および `AGENTS.md` へ漏れなく更新・反映し、確実に Git Push すること。
  3. 既存の決定仕様やテキストを勝手に要約・削除・省略することを 100% 物理全廃する。

---

## 7. ⚖️ 【安易な迎合 (イエスマン) の絶対禁止 ✕ 3大構造化考察の義務】
* **反省点**: ユーザーの意見に対して「素晴らしいです！」とベタ褒めし、思考停止でイエスマンの回答を行った。
* **鉄則**:
  1. 形だけのベタ褒めや前言撤回、安易な迎合を絶対禁止とする。
  2. ユーザーから意見・提案を求められた際は、必ず **「① 懸念点・問題点 ➔ ② 対応策・解決調整案 ➔ ③ 冷徹な総括判定」** の 3 大構造化フォーマットに従い、裏側のゲームバランスまで冷静かつ深層に考察して回答すること。

---

## 8. 🛑 【Git Push の事前完全提示・絶対承認の義務】(無断Pushの100%禁止)
* **反省点**: ユーザーの明示的な確認・許可を得ずに `git push` コマンドを自動実行した。
* **鉄則**:
  1. Git Push を行う際は、事前に変更内容（Diff、コミットメッセージ、送信対象ブランチ名）を完全に提示すること。
  2. 提示後、ユーザーから明確な確認・承認（「PushしてOK」等）の返答を得るまで、絶対に `git push` コマンドを実行してはならない。

---

> 🎮 **【本番制作目標】**:
> 本Webプロトタイプ (`game/index.html`) は高速テスト検証用であり、**最終ゴールは Unity Engine (C# / ScriptableObject) での本番アプリ開発** である。すべてのデータと設計はC#クラスへ 1対1 移植できる構造を維持すること。
