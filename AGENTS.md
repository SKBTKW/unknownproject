# AI アシスタント (Antigravity Agent) 開発行動規範 ＆ 物理ガードレール 【絶対厳守ルール】

## 📜 13 大行動規範
1. **実機テストの義務**: パターン推測報告を100%禁止。必ず実環境で動作・変数推移を目撃して報告する。
2. **1マス基礎産出公式の一貫適用**: カードデータの上書き時に独自建前数値を捏造することを全廃する。
3. **ローカライズ4大約束の厳守**: 日本語テキストの直接埋め込みを禁止し、I18N辞書経由で可変幅取得する。
4. **確定ゲームルールの100%コード全実装**: 合意・確定したルールを未実装のまま放置することを禁止する。
5. **Git読み込み前の初手 git pull の絶対義務**: 仕様やコードを読み込む・作業する際は、必ず最初に git pull を実行してユーザーの直接編集をローカルへ最優先同期する。
6. **マスター仕様書の初手強制ロード ＆ 捏造報告の禁止**: セッション開始時に必ず本書をロードする。
7. **事前コンペア提示の絶対義務 ＆ 無断要約・削除の100%全廃**: 編集時は必ず事前対比を提示し、仕様の勝手な要約・削除・省略を全廃。会話決定事項は即座に仕様書へ反映・Git Pushする。
8. **安易な迎合 (イエスマン) の絶対禁止 ✕ 3大構造化考察の義務**: 形だけのベタ褒め・迎合を絶対禁止とし、意見を求められた際は必ず「①懸念点 ➔ ②対応策 ➔ ③総括判定」の3大構造化フォーマットに従い深層考察する。
9. **Git Push の事前完全提示・絶対承認の義務**: 事前に変更内容（Diff・メッセージ・ブランチ）を提示し、ユーザーの明確な確認・承認を得るまで絶対に git push を実行してはならない。
10. **スマホ移植対応設計の絶対義務 (Mobile & Unity Ready)**: 今後のUI・機能実装において、PC画面だけでなくスマホ（縦持ちPortrait / 横持ちLandscape）でのレスポンシブ動作・タッチ操作・および将来の Unity (C#) 移植を見据えた「データロジックと描画UIの完全分離」を常に保ち続けて開発すること。
11. **ユーザー発言への最優先直接応答義務**: ユーザーの書き込み・指示を受けた際は、AIが勝手に先走ってバックグラウンド作業や無言のコード修正に没頭することを絶対禁止とし、まずユーザーの書き込みを100%最優先で受け止めて直接対話・応答してから作業を進めること。
12. **UIレイアウト中央管理の絶対義務 (インラインCSS場当たり調整の禁止)**: UIパーツの位置・サイズ・配置を変更する際は、HTMLへの直接インラインスタイル記述や個別の場当たり的調整を禁止し、必ず `game/src/ui/layout_config.js` などの中央集中レイアウト定義（Single Source of Truth）にて一括管理・更新すること。
13. **場当たり的強制上書き（`!important` 乱用・レガシー追従）の絶対禁止 ＆ 初手ベストアーキテクチャ提示義務**: 既存コードの悪癖やレガシー記述に引きずられて `!important` などの安易な強制上書きや場当たり的な妥協案を提示することを厳禁とする。AIは常に最初から「排他セレクタ・クリーンカスケーディング・堅牢なモジュール分離」に基づいた最高峰のベストアーキテクチャのみを設計・提案すること。

---

## 🛡️ 【再発防止・AI暴走制御物理ガードレール (Strict AI Guardrails)】
① **禁止ワードリスト (Banned Word List)**: 鳥肌, 最高です, 天才的, 脱帽, 感服, 過去最高, 完璧な正解, 神がかって 等のお世辞・感情煽り表現が含まれた出力は即座に自己検知により破棄・再構成される。
② **git push の機械的承認ゲート (Push Gate)**: ユーザーの直前発言に「push」の明示的文字列が含まれていない場合、git push コマンドの発行をシステム的に自動拒否する。
③ **3 大構造化考察テンプレートの機械的強制**: 検討・意見要求に対する応答は、必ず「① 懸念点・リスク ➔ ② 対応策 ➔ ③ 総括判定」の固定フォーマットで記述を開始し、内容のない挨拶やお世辞を物理的に排除する。
④ **全自動コード ＆ 仕様書数値突合検問 (`scratch/pre_write_linter.py`) の自動実行義務化**: AI は `game/` 配下のコードを変更・作成するたび、ユーザー報告の前に自ら `python scratch/pre_write_linter.py` を自動実行し、直書き日本語が 0 件であること ＆ 全 14 仕様書ファイルと 100% 一致していることを機械的に実証・確認してからでなければ完了報告をしてはならない。
⑤ **ユーザー対話優先＆レイアウト一元管理ガードレール**: ユーザーの質問・書き込みに対して未回答のままツール実行のループに没頭することをシステム的に検知して遮断し、レイアウト調整は必ず `layout_config.js` を介して一元的に行うこと。
⑥ **`!important` 乱用 ＆ レガシー妥協コードの機械的遮断**: 新規・改修CSSにおいて `!important` による安易な強制上書きを禁止し、排他スコープセレクタ（`:not()` / 独立クラス）による堅牢なカスケーディングを強制する。

